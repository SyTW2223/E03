{
  "scripts": {
    "test": "npm run test --prefix ./my-project && npm run test --prefix ./my-project-server"
  },
  "dependencies": {
    "dotenv": "^16.2.0",
    "os-browserify": "^0.3.0",
    "path-browserify": "^1.0.1"
  },
  "devDependencies": {
    "jest": "^29.5.0"
  }
}
